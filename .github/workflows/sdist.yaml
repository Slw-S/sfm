name: Build Source Distribution

on:
  workflow_dispatch:

jobs:
  build-sdist:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install UV
        run: |
          curl -fsSL https://github.com/astral-sh/uv/releases/latest/download/uv-installer.sh | sh

      - name: Create UV Virtual Environment
        run: |
          uv venv .venv

      - name: Install Build Tools & Lock Dependencies
        run: |
          .venv/bin/uv pip install --upgrade setuptools build
          .venv/bin/uv lock

      - name: Sync to Locked Dependencies
        run: |
          .venv/bin/uv sync --locked

      - name: Build Source Distribution (sdist)
        run: |
          .venv/bin/python -m build --sdist

      - name: Upload sdist Artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz
